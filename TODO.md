# TODO

- [ ] Add missing `WatchlistEntry` model to `models.py` (UUID-based, with `public`, `date_added`, FKs, and `film` relationship).
- [ ] Rerun `pytest -q` and ensure watchlist tests pass.
- [ ] If rebase/UUID conflict details differ from current `pr-response.md`, update Comment 6 section to match actual conflict + verification.

